// Cloud sync stub — Dropbox/Google Drive
// Post-launch implementation

pub fn sync_to_cloud(_path: &str) -> Result<(), Box<dyn std::error::Error>> {
    // Stub — no real API calls
    Err("Cloud sync not yet implemented".into())
}
